const numero = 4
let tipoNumero = (numero > 0) ? "Positivo" : "Negativo o 0";
console.log(tipoNumero)


const cadena = "carallo que largo"
let longitudCadena = (cadena.length>5)? "largo": "corto";
console.log(`${longitudCadena}`)

function esPar(numero){
    solucion = numero % 2 = 0 ?: 'true' : 'false'
    return solucion
}
const numero = 13
console.log(solucion)
...